# DiscOS ↔ EvidenceOS Compatibility Pin

This repository is the canonical upstream for EvidenceOS.

- Canonical upstream repository: `https://github.com/jverdicc/EvidenceOS`
- FORC submission pin: `forc-submission-v1.0`
- Vendor lock file: `scripts/evidenceos_vendor.lock`

Downstream repositories that vendor EvidenceOS (for example DiscOS tooling) should use the same upstream URL and pin.
